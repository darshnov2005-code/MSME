"use client";import {useEffect,useState} from "react";type V={name:string,status:string,credit:number};export default function VendorMaster(){const[v,setV]=useState<V[]>([]);const[name,setName]=useState("");const[status,setStatus]=useState("Review");useEffect(()=>setV(JSON.parse(localStorage.getItem("msmeVendors")||"[]")),[]);function add(){if(!name.trim())return;const n=[...v,{name:name.trim(),status,credit:45}];setV(n);localStorage.setItem("msmeVendors",JSON.stringify(n));setName("")}return <><header className="topbar"><div><div className="topTitle">Vendor Master</div><div className="topSub">Classify MSE applicability manually — no Udyam verification</div></div></header><section className="card"><div className="formRow"><div className="field"><label>Vendor</label><input className="input" value={name} onChange={e=>setName(e.target.value)}/></div><div className="field"><label>MSE status</label><select className="select" value={status} onChange={e=>setStatus(e.target.value)}><option>MSE</option><option>Non-MSE</option><option>Review</option></select></div><div className="field"><label>Credit Days</label><input className="input" defaultValue="45"/></div></div><button className="btn btnPrimary" style={{marginTop:12}} onClick={add}>Add Vendor</button></section><section className="card"><div className="tableWrap"><table className="table"><thead><tr><th>Vendor</th><th>MSE Status</th><th>Credit Days</th></tr></thead><tbody>{v.length?v.map((x,i)=><tr key={i}><td>{x.name}</td><td><span className="pill pillAmber">{x.status}</span></td><td>{x.credit}</td></tr>):<tr><td colSpan={3}>No vendors added.</td></tr>}</tbody></table></div></section></>}