"use client";import {useEffect,useState} from "react";
export default function Clients(){const [name,setName]=useState("");const [clients,setClients]=useState<string[]>([]);useEffect(()=>setClients(JSON.parse(localStorage.getItem("msmeClients")||"[]")),[]);function add(){if(!name.trim())return;const next=[...clients,name.trim()];setClients(next);localStorage.setItem("msmeClients",JSON.stringify(next));setName("")}return <><header className="topbar"><div><div className="topTitle">Clients</div><div className="topSub">Manage multiple entities and financial years</div></div></header><section className="card"><div className="formRow"><div className="field"><label>Client / Entity name</label><input className="input" value={name} onChange={e=>setName(e.target.value)} placeholder="ABC Pvt Ltd"/></div><div className="field"><label>Financial Year</label><select className="select"><option>2025-26</option><option>2024-25</option><option>2026-27</option></select></div><div className="field"><label>&nbsp;</label><button className="btn btnPrimary" onClick={add}>+ Add Client</button></div></div></section><section className="card"><h2 className="sectionTitle">Client register</h2><div className="tableWrap"><table className="table"><thead><tr><th>Entity</th><th>FY</th><th>Status</th><th>Action</th></tr></thead><tbody>{clients.length?clients.map(c=><tr key={c}><td>{c}</td><td>2025-26</td><td><span className="pill pillAmber">In Review</span></td><td><a href="/paste">Open</a></td></tr>):<tr><td colSpan={4}>No clients added yet.</td></tr>}</tbody></table></div></section></>}