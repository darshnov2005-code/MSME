"use client";import {useState} from "react";export default function ExportPage(){const[msg,setMsg]=useState("");function exportCsv(){const data=JSON.parse(localStorage.getItem("msmeRows")||"[]");const lines=[["Sr No","Amount","Doc Date","Credit Period","Clearing / Payment Date"],...data.map((r:any)=>[r.sr,r.amount,r.doc?new Date(r.doc).toLocaleDateString("en-IN"):"",r.credit,r.pay?new Date(r.pay).toLocaleDateString("en-IN"):""])];const csv=lines.map((r:any[])=>r.join(",")).join("\n");const a=document.createElement("a");a.href=URL.createObjectURL(new Blob([csv],{type:"text/csv"}));a.download="MSME_Working_Paper.csv";a.click();setMsg(data.length+" rows exported.");}return <><header className="topbar"><div><div className="topTitle">Export</div><div className="topSub">Export supporting schedules</div></div></header><section className="card"><button className="btn btnPrimary" onClick={exportCsv}>Export Working Paper CSV</button>{msg&&<p className="muted">{msg}</p>}<div className="note" style={{marginTop:12}}>XLSX/PDF generation is the next export layer; the audit schedules are already structured for it.</div></section></>}